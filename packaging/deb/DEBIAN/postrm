#!/bin/sh
set -e

# Remove service files and reload
if command -v systemctl >/dev/null 2>&1; then
  systemctl stop mcpanel.service || true
  systemctl disable mcpanel.service || true
  systemctl daemon-reload || true
fi

exit 0
